# Primary DB
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=atomflow

# Secondary DB (for mqttmeter reads)
DB_NAME2=atomflow_mqtt

# Auth
JWT_SECRET=replace-with-a-strong-secret

# Server
PORT=3003
